<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/margin_cardview"
    android:layout_marginRight="@dimen/margin_cardview"
    android:layout_marginTop="@dimen/margin_cardview"
    android:layout_marginBottom="@dimen/margin_cardview"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!--suppress AndroidLintContentDescription -->
        <com.example.xyzreader.ui.DynamicHeightNetworkImageView
            android:id="@+id/thumbnail"
            android:layout_width="@dimen/placeholder_image_width"
            android:layout_height="@dimen/placeholder_image_height"
            android:background="@color/photo_placeholder" />

        <TextView
            android:id="@+id/article_title"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/padding_normal_article_title"
            android:paddingTop="@dimen/padding_normal_article_title"
            android:paddingRight="@dimen/padding_normal_article_title"
            android:paddingBottom="@dimen/padding_small_article_title"
            android:textColor="@color/colorPrimaryText"
            android:ellipsize="end"
            android:maxLines="@integer/max_line_article_title" />

        <TextView
            android:id="@+id/article_subtitle"
            android:textAppearance="@style/TextAppearance.AppCompat.Small"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/padding_article_subtitle"
            android:paddingRight="@dimen/padding_article_subtitle"
            android:paddingBottom="@dimen/padding_article_subtitle"
            android:textColor="@color/colorSecondaryText"
            android:maxLines="@integer/max_line_article_subtitle"
            android:ellipsize="end" />
    </LinearLayout>

</android.support.v7.widget.CardView>
